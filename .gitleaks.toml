title = "polyglot-nlp-toolkit gitleaks config"

[extend]
useDefault = true

[[rules]]
id = "private-key"
description = "Private key"
regex = '''-----BEGIN (RSA|DSA|EC|OPENSSH|PRIVATE) KEY-----'''
tags = ["key", "private"]
